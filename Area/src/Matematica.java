
public class Matematica {

	int calculararea(int x) { //declarando método para calcular a área com o parâmetro x (o lado)

		return x * x;
	}
        //declarando método para calcular a área com o parâmetro x e y(a base e a altura)
	double calculararea(double x, double y) {

		return (x * y) / 2;
	}
	
        //declarando método para calcular a área com o parâmetro x e y (a base e a altura)
	int calculararea(int x, int y) {

		return x * y;
	}

}
